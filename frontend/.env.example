# 前端构建时环境变量（必须 VITE_ 前缀才会被 Vite 注入到浏览器端）。
# 复制为 .env.local 后覆盖默认值（.env.local 已在 .gitignore 中）。
# SSOT：frontend/src/services/api/client.ts

# 后端 API 根路径（含 /api 前缀）
VITE_API_BASE_URL=http://localhost:8000/api
